# Data Flow

Workbook + evidence upload
→ backend analysis API
→ evidence results
→ workbook observations
→ gap tickets/findings
→ Streamlit UI review

Streamlit should depend on internal adapted models, not raw backend payloads.
